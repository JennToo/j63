rule:
  # Mostly using entity method, but component declarations are needed for IP
  instantiation_034:
    disable: true

  # Default assignments are very useful in simulation
  signal_007:
    disable: true
  port_012:
    disable: true

  # Normally "Hungarian Notation" is overkill. But in this context, it is handy
  port_025:
    disable: false
    suffixes: ["_i", "_o", "_io", "_ni", "_no"]
  process_600:
    disable: false
    suffixes: ["_p"]
